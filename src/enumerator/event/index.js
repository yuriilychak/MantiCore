import KEYBOARD_EVENT from "./KeyboardEvent";
import LOADER_EVENT from "./LoaderEvent";
import SYSTEM_EVENT from "./SystemEvent";

/**
 * @desc Namespace that contains some game events.
 * @namespace event
 * @memberOf MANTICORE.enumerator
 */

export default{
    KEYBOARD_EVENT,
    LOADER_EVENT,
    SYSTEM_EVENT,
}