/**
 * @desc Enum with button ids on mouse event;
 * @memberOf mCore.enumerator
 * @enumerator {int}
 * @name MOUSE_BUTTON
 */

export default {
    NONE: -1,
    LEFT: 0,
    RIGHT: 2,
    WHEEL: 4
}
