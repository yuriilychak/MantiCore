/**
 * @desc Enumerator for define bundle type.
 * @enum {int}
 * @readonly
 * @memberOf MANTICORE.enumerator
 */

const BUNDLE_TYPE = {
    NONE: 0,
    ASSET: 1,
    LOCALE: 2
};

export default  BUNDLE_TYPE;